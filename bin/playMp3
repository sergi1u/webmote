if [ $# -eq 0 ]; then
  echo "Faltan parametros";
else
  echo "Playing MP3 $1"
  #dirige el audio hacia jack
  sudo modprobe snd_bcm2835
  sudo amixer cset numid=3 1

  mpg123 -C $1

  # redirge audio al HDMI
  sudo amixer cset numid=3 2;
fi
